/* Basic model class for a TeamMember */
export class TeamMember {

    // Unique identifier
    id: number;

    // user Id
    userId: number;

    // project id
    projectId: number;

    // Name
    name: string;

    // Name
    teamLeader: boolean;
}